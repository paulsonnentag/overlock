# Overlock - stitch together untrusted components, safely

This is an experiment to explore how to have loosely coupled components in a safe way while still allowing deep interactions between the components.

Goals:

- allow to run untrusted code in the same
